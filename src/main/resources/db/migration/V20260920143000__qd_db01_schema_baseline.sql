-- Flyway creates the uteexpress schema and records history in that schema.
-- Business tables belong to their module owners and subsequent tasks.
COMMENT ON SCHEMA uteexpress IS 'UTEExpress application schema; managed by Flyway';
